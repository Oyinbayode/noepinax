{
  "name": "noepinax",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "generate-wallets": "tsx scripts/generate-wallets.ts",
    "seed": "tsx scripts/seed-agents.ts",
    "register": "tsx scripts/register-identities.ts",
    "start": "tsx scripts/run-agents.ts",
    "deploy:contracts": "cd contracts && npx hardhat run scripts/deploy.ts --network baseSepolia"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "keccak",
      "secp256k1",
      "sharp"
    ]
  }
}